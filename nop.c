#include "monty.h"

/**
 * nop - doesn’t do anything.
 * @stack: input structure
 * @line_number: input line of exec
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
